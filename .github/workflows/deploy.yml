name: Run WhatsApp Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"  # restart every 6 hours
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run bot
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          npm install -g localtunnel
          echo "Using Groq Key: $GROQ_API_KEY"
          node index.js &
          lt --port 3000 --subdomain zani-bot &
          sleep infinity
